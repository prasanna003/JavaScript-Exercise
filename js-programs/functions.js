//Write a function isEven that returns true if a number is even, and false otherwise.

function isEven(number) {
  return number % 2 == 0 ? true : false;
}

console.log(isEven(4)); // true
console.log(isEven(5)); // false
